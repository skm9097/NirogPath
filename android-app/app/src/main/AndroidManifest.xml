<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="NirogPath"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NirogPath"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.onboarding.OnboardingActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.dashboard.MainActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.health.AddReadingActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.medications.AddMedicationActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.ai.AiConsultActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.guardian.GuardianActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.doctor.DoctorReportActivity"
            android:screenOrientation="portrait" />

        <receiver
            android:name=".utils.ReminderReceiver"
            android:exported="false" />

    </application>

</manifest>
